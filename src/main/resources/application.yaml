spring:
  datasource:
    url: jdbc:h2:mem:camunda-h2-database
    driverClassName: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true
      path: /h2-console
camunda:
  bpm:
    admin-user:
      password: demo
      id: demo